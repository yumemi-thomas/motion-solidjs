/**
 * Global package configuration. Reserved for future feature flags.
 *
 * The previous `motionAttribute` field was removed when AnimatePresence
 * moved to a per-instance runExit registry.
 */
export const motionGlobalConfig: Record<string, never> = {}
